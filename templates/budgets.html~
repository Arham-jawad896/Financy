{% extends 'dashboard.html' %}

{% block page_content %}
<div class="budgets-container">
    <h1>Budgets</h1>

    <div class="budget-overview">
        <h2>Budget Overview</h2>

        {% for budget in budgets %}
        <div class="card">
            <h3>{{ budget.category }}</h3>
            <p>Budget Limit: ${{ budget.limit }}</p>
            <p>Spent Amount: ${{ budget.spent }}</p>
            <div class="progress">
                {% set progress = (budget.spent / budget.limit * 100) %}
                <div class="progress-bar" style="width: {{ progress }}%; background-color: {% if progress >= 100 %} #c0c0c0 {% else %} #a0a0a0 {% endif %};"></div>
            </div>
            <button onclick="editBudget('{{ budget.category }}')">Edit</button>
            <button onclick="deleteBudget('{{ budget.category }}')">Delete</button>
        </div>
        {% endfor %}
    </div>

    <div class="add-budget">
        <h2>Add New Budget</h2>
        <form action="{{ url_for('add_budget') }}" method="POST">
            <label for="category">Category:</label>
            <input type="text" id="category" name="category" required>
            <label for="budgetlimit">Budget Limit:</label>
            <input type="number" name="budget_limit" placeholder="Enter budget limit" required>
            <label for="spent">Spent Amount:</label>
            <input type="number" id="spent" name="spent" required>
            <label for="start-date">Start Date:</label>
            <input type="date" id="start-date" name="start_date" required>
            <label for="end-date">End Date:</label>
            <input type="date" id="end-date" name="end_date" required>
            <button type="submit">Add Budget</button>
        </form>
    </div>

    <div class="budget-history">
        <h2>Budget History</h2>
        <table>
            <thead>
                <tr>
                    <th>Category</th>
                    <th>Budget Limit</th>
                    <th>Spent Amount</th>
                    <th>Date Range</th>
                </tr>
            </thead>
            <tbody>
                {% for history in budget_history %}
                <tr>
                    <td>{{ history.category }}</td>
                    <td>${{ history.limit }}</td>
                    <td>${{ history.spent }}</td>
                    <td>{{ history.start_date }} - {{ history.end_date }}</td>
                </tr>
                {% endfor %}
            </tbody>
        </table>
    </div>
</div>

<style>
    .budgets-container {
        margin: 20px;
        font-family: Arial, sans-serif;
    }
    h1, h2 {
        color: #333;
    }
    .card {
        border: 1px solid #ccc;
        border-radius: 5px;
        padding: 15px;
        margin-bottom: 15px;
        background-color: #f9f9f9;
    }
    .progress {
        background-color: #e0e0e0;
        border-radius: 5px;
        overflow: hidden;
    }
    .progress-bar {
        height: 10px;
    }
    table {
        width: 100%;
        border-collapse: collapse;
    }
    th, td {
        padding: 10px;
        border: 1px solid #ccc;
        text-align: left;
    }
    th {
        background-color: #f2f2f2;
    }
    form {
        margin: 20px 0;
    }
    label {
        display: block;
        margin-bottom: 5px;
    }
    input {
        width: 100%;
        padding: 8px;
        margin-bottom: 10px;
        border: 1px solid #ccc;
        border-radius: 4px;
    }
    button {
        padding: 10px 15px;
        background-color: #666;
        color: white;
        border: none;
        border-radius: 4px;
        cursor: pointer;
    }
    button:hover {
        background-color: #555;
    }
</style>

<script>
    function editBudget(category) {
        // Redirect to edit budget page or open a modal
        alert('Edit ' + category);
    }

    function deleteBudget(category) {
        // Confirm and delete the budget
        if (confirm('Are you sure you want to delete ' + category + '?')) {
            // Add delete logic here
            alert('Deleted ' + category);
        }
    }
</script>

{% endblock %}
